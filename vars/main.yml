---
# vars file for docker
docker_packages:
  default:
    - docker
  CentOS:
    - docker-io
  Debian:
    - docker-engine
  Ubuntu:
    - docker-engine

docker_service: docker

docker_pip_modules:
  - name: docker-compose
