---
language: python

install:
  - go get github.com/digitalocean/doctl/cmd/doctl
  - ./go/bin/doctl auth init

script:
  - doctl compute droplet list

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
  email: false
