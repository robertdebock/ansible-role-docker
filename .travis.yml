---
language: python

env:
  matrix:
    - IMAGE=debian-8-x64
    - IMAGE=centos-7-x64

install:
  - wget https://raw.githubusercontent.com/robertdebock/digitalocean/master/do.sh
  - chmod +x do.sh
  - do.sh -i
  
script:
  - echo "Creating droplets."

after_script:
  - echo "Removing ssh key"
  - echo "Remove droplets."

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
  email: false
